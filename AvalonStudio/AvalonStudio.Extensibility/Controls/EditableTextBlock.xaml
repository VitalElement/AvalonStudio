<Styles  xmlns="https://github.com/avaloniaui" xmlns:cont="clr-namespace:AvalonStudio.Controls;assembly=AvalonStudio.Extensibility">
  <Style Selector="cont|EditableTextBlock">
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="0"/>
    <Setter Property="Template">
      <ControlTemplate>
        <Grid>
          <TextBlock Text="{TemplateBinding Text, Mode=TwoWay}" IsVisible="{TemplateBinding !InEditMode}" />
          <TextBox Text="{TemplateBinding Text, Mode=TwoWay}" IsVisible="{TemplateBinding InEditMode}" Name="PART_TextBox" />
        </Grid>
      </ControlTemplate>
    </Setter>
  </Style>
</Styles>