language: csharp
sudo: required
services:
  - docker
os:
  - linux
mono:
  - latest
solution: AvalonStudio.sln
before_install:
  - cd AvalonStudio 
  - sudo ./dependencies.sh

install:  
script:
  - sudo ./buildmono.sh

  #- if [ "$TRAVIS_BRANCH" == "master" ]; then
  - git clone https://github.com/VitalElement/AvalonBuild.git
  - cd AvalonBuild
  - git rm -rf .
  - git clean -fxd
  - cd ..
  - pwd
  - cp -a /AvalonStudioBuild/bin/x86/Release/. ./AvalonBuild
  - cd AvalonBuild
  - git add .
  - git commit -m 'new CI revision.'
  - git push -f -q https://VitalElement:$GH_TOKEN@github.com/VitalElement/AvalonBuild master &2>/dev/null
  - cd ..
  - cd AvalonStudioBuild
  - docker build --no-cache -t vitalelement/avalon .
  # fi

after_success:
  #- if [ "$TRAVIS_BRANCH" == "master" ]; then
  #  docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
  #  docker push vitalelement/avalon;
  #  fi
notifications:
  webhooks:
    urls:
      - https://webhooks.gitter.im/e/abf03510b7719b383e2b
    on_success: change  # options: [always|never|change] default: always
    on_failure: always  # options: [always|never|change] default: always
    on_start: never     # options: [always|never|change] default: never